{
  "compilerOptions": {
    "types": ["jest", "node"],  // 타입 배열에서 'kakao.maps.d.ts' 제거
    "jsx": "react",
    "esModuleInterop": true,  // 이 옵션을 추가하여 모듈 간 호환성 개선
    "allowSyntheticDefaultImports": true  // ES 모듈을 CommonJS에서 사용할 수 있도록 설정
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "node_modules/kakao.maps.d.ts"  // kakao.maps.d.ts 파일을 포함
  ]
}
